# Note
For qn 1-7, In the event that you are not able to compile the files normally like `javac BubbleTeaTest.java`, please try:

- With the -cp flag to compile : `javac -cp ./ BubbleTeaTest.java` 
- With the -cp flag to run: `java -cp ./ BubblTeaTest` 

